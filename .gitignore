venv/
.env
__pycache__/
*.pyc
*.db
reports/
# .ai-workspace/ is per-project runtime state the tool produces when it is
# USED on a project (scan profile, ChromaDB memory, generated tests, logs).
# It is not part of the tool's source — never tracked, at any depth.
.ai-workspace/
